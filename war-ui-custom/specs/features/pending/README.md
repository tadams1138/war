# Pending feature files

`template-contract.feature` — 11 scenarios specifying the contract a custom UI bundle
must satisfy, moved here when Gherkin was removed from `specs/war-ui-custom-spec.md`.

**The `war-ui-custom` project does not exist yet.** These scenarios describe what it must
do when it is built; this directory exists so they sit with the project they belong to
rather than inside a prose specification.
