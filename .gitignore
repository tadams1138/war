# Fire Team scratch — findings and run logs are process records,
# true only until acted upon. See constitution Article V.
.claude/reviews/
.claude/runs/

# Isolated workspaces for subagent-driven implementation work
.worktrees/

# Node — unanchored so they match in war-api/ and war-ui-default/ alike
node_modules/
dist/
dist-ssr/
coverage/
*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Secrets
.env

# Playwright (war-ui-default)
test-results/
playwright-report/
blob-report/
playwright/.cache/

# generate:api's local OpenAPI artifact (war-ui-default/package.json,
# war-api/scripts/dumpOpenApi.ts) -- a build-time intermediate, not source.
/war-ui-default/openapi.json

# Terraform (war-infra)
**/.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
