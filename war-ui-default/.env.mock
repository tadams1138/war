# Loaded by `vite build --mode mock` (npm run build:mock). Turns on the MSW
# browser worker so the Playwright acceptance suite (tests/acceptance/) can
# run against mocked responses without a live war-api — never set in a real
# deploy (the spec).
VITE_API_MOCKING=enabled
