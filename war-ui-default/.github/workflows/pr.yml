name: PR Checks

on:
  pull_request:
    branches: [master]

jobs:
  pipeline:
    uses: tadams1138/war-infra/.github/workflows/ui-default.yml@master
    with:
      deploy: false
    secrets: inherit
